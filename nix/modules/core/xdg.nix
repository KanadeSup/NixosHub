{ ... }:

{
  environment.sessionVariables = {
    XDG_CACHE_HOME = "$HOME/.data/cache";
    XDG_CONFIG_HOME = "$HOME/.config";
    XDG_DATA_HOME = "$HOME/.data/share";
    XDG_STATE_HOME = "$HOME/.data/state";
  };
  
}
